let num1 = 20, num2 = 30, num3 = 40;
        let nums = [20, 30, 40]
        console.log(nums[1])

        // if we use below code its will shown undefined becoz value is not defined
        console.log(nums[5])

    nums[5] = 100;
    console.log(nums.length)
    // this will show 6 but when we see the result to shown between two will be undefined